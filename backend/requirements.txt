fastapi==0.115.6
uvicorn[standard]==0.27.1
# >=2.0.36: Python 3.13+/3.14 TypingOnly (__static_attributes__, etc.)
SQLAlchemy>=2.0.36,<2.1
# >=2.7.4: fixes ForwardRef._evaluate / recursive_guard for Python 3.12.4+ and 3.13/3.14 (pydantic-core sdist build)
pydantic>=2.7.4,<3
python-multipart==0.0.9
opencv-contrib-python==4.9.0.80
numpy==1.26.4
dlib-bin
face-recognition==1.3.0
# Required at runtime: `import face_recognition` looks for this model bundle (often not pulled in reliably on all platforms).
face-recognition-models>=0.3.0
# face_recognition_models uses pkg_resources; setuptools 82+ dropped pkg_resources → pin below 82 (otherwise `import face_recognition` prints the git+ install hint and fails).
setuptools>=69.0.0,<81
requests==2.31.0
openpyxl==3.1.2
cryptography>=42.0.0

# tests
pytest>=8.0.0
httpx>=0.27.0

